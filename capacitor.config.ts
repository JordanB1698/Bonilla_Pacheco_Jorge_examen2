import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bonilla_Pacheco_Jorge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
